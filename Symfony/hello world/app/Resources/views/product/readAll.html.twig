{% extends 'base.html.twig' %}

{% block body %}
  {% for product in products %}
    <ul>
      <li>
        <div>
          {{product.name}}
        </div>
        <div>
          {{product.price}}
        </div>
      </li>
    </ul>
  {% endfor %}
{% endblock %}

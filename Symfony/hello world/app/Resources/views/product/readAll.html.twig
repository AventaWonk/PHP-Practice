{% extends 'base.html.twig' %}

{% block body %}
  <div class="row">
    {% for product in products %}
      <ul>
        <li>
          <div>
            {{product.name}}
          </div>
          <div>
            {{product.price}}
          </div>
          <a href="/delete/{{product.id}}">x</a>
        </li>
      </ul>
    {% endfor %}
  </div>
{% endblock %}

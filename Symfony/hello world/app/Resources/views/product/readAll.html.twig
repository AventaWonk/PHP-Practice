{% extends 'base.html.twig' %}

{% block body %}
  <div class="row">
    {% for product in products %}
      <ul>
        <li>
          <div>
            {{product.name}}
          </div>
          <div>
            {{product.price}}
          </div>
          <a href="/delete/{{product.id}}">D</a>
          <a href="/update/{{product.id}}">U</a>
        </li>
      </ul>
    {% endfor %}
  </div>
{% endblock %}
